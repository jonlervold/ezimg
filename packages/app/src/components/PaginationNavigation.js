const PaginationNavigation = () => {
  return (
    <div>
      Display Per Page: <input></input> (implement this later)
      <br />
      <button>Back</button>
      Images x - x<button>Forward</button> (implement this later)
    </div>
  );
};

export default PaginationNavigation;
